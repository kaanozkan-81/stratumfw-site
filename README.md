# Stratum Gateway — Corporate Website

The source code for the [stratumfw.com](https://stratumfw.com) corporate website — the public-facing presence for **Stratum Gateway**, an enterprise-grade, self-hosted firewall and network security platform.

This repository contains only the website. The Stratum Gateway platform itself is maintained separately.

---

## About Stratum Gateway

Stratum Gateway is an open-source firewall and network security platform that runs entirely on your own hardware or virtual infrastructure. It consolidates more than twenty network functions — routing, multi-WAN failover, VPN, intrusion detection, content filtering, and traffic shaping — into one system managed from a single web panel.

- **Self-hosted** — your data never leaves your premises
- **Open source** — no per-user licensing, no recurring fees
- **Debian-based** — built on Debian 13 (trixie) with Linux 6.12 LTS

Learn more at [stratumfw.com](https://stratumfw.com).

---

## About this repository

This is a **Hugo** static site. Every page is either a Markdown file (`.md`) or a raw HTML file (`.html` — used only when a page needs full control over markup, like the download page).

### Structure
